[project]
name = "window_manager"
version = "0.1.0"
description = "Window manager and monitor for Windows"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "psutil>=7.2.2",
    "pywin32>=311",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
